{% render 'partials/head.liquid' %}

<section class="visitekaartje">
    <header class="header">
        <img src="">
        <h1>{{ person.name }}</h1>
    </header>
</section>

{% render 'partials/foot.liquid' %}
